[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "larsa-core"
version = "0.1.0"
description = "Rust-accelerated SRFM physics for LARSA trading strategy"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
